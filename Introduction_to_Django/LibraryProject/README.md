# LibraryProject

## Purpose
Learning Django: project scaffold and dev server.

## How to run (local)
1. Create & activate virtualenv
2. Install dependencies: `pip install django`
3. Run server: `python manage.py runserver`
4. Visit http://127.0.0.1:8000/
